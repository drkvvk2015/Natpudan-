[pytest]
asyncio_mode = auto
addopts = --ignore=tests/test_openai.py --ignore=tests/test_auth.py
filterwarnings =
    ignore::DeprecationWarning:pytest_asyncio.plugin
markers =
    integration: marks tests that exercise cross-component integration paths
