// Configuration for Natpudan AI Medical Assistant

const config = {
    // Backend API URL
    // Change this to your backend server URL
    API_URL: 'http://localhost:8000',
    
    // Enable/disable backend features
    // Set to false to use local demo mode without backend
    USE_BACKEND: true,
    
    // Timeout for API requests (milliseconds)
    API_TIMEOUT: 30000,
};
